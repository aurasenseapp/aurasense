/* Aurasense — the Gate's two public values.
   These live here so they survive every future whole-file replacement of index-mapbox.html.
   Both are public by design: the publishable key is the key the browser is meant to carry.
   Nothing secret lives here — the secret key never appears in this file, or any file on the web.
   Found in Supabase → Project Settings → API Keys (Publishable key), and → Data API (API URL). */
window.AURASENSE_GATE = {
  url : 'https://iwuqcmhlbjyjhqexbeyy.supabase.co',
  key : 'sb_publishable_trDTjqSBIqdwSyd2EVtJHw_JD2exVmr'
};
